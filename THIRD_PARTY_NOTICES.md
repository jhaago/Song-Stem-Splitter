# Third-party notices

Song Stem Splitter depends on third-party open-source packages. The application itself does not redistribute their source licenses here; packaged releases must include the license files required by each bundled dependency.

The initial separation backend uses **Demucs**, which is distributed under the MIT License. The pretrained model is downloaded from the upstream Demucs model host on first use.

Before public distribution, regenerate a complete dependency/license inventory from the exact locked build environment and include it with each desktop package.
